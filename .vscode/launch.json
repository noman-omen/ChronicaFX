{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Launch MainApp",
            "request": "launch",
            "mainClass": "MainApp",
            // The below "vmArgs" setting tells Java where to find the JavaFX modules needed to run the program. Change the path to match your own JavaFX SDK location.
            "vmArgs": "--module-path \"E:\\JavaFX\\lib\" --add-modules javafx.controls,javafx.fxml,javafx.graphics"
        }
    ]
}
